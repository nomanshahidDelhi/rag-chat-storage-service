spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:ragchat}
    username: ${POSTGRES_USER:ragchat}
    password: ${POSTGRES_PASSWORD:ragchat}
  jpa:
    show-sql: true

logging:
  level:
    com.nagarro.ragchat: DEBUG
